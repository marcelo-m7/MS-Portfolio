name: CI Pipeline – Build, Test & Lint

on:
  push:
    branches: ['*']
  pull_request:
    branches: ['*']
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  checks: write
  pull-requests: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  setup:
    name: Setup Environment
    runs-on: ubuntu-latest
    outputs:
      pnpm-cache-key: ${{ steps.pnpm-cache-key.outputs.key }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Compute pnpm cache key
        id: pnpm-cache-key
        run: echo "key=pnpm-modules-${{ runner.os }}-${{ hashFiles('**/pnpm-lock.yaml') }}" >> $GITHUB_OUTPUT

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Record pnpm and node versions
        run: |
          node -v
          pnpm -v

      - name: Setup Python (if needed)
        if: hashFiles('**/requirements.txt') != ''
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Python dependencies (if needed)
        if: hashFiles('**/requirements.txt') != ''
        run: pip install -r requirements.txt

  lint:
    name: Lint
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run ESLint
        env:
          CI: true
        run: pnpm lint

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: setup
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run tests with coverage
        env:
          CI: true
        run: pnpm test:coverage

      - name: Upload coverage artifact
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-coverage
          path: |
            coverage/**
          if-no-files-found: ignore
          retention-days: 7

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [setup, lint, test]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build project
        env:
          CI: true
        run: pnpm build

      - name: Upload production build
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-dist
          path: |
            dist/**
          if-no-files-found: error
          retention-days: 7

  preview_validation:
    name: Deployment Preview Validation
    runs-on: ubuntu-latest
    needs: [lint, test]
    if: |
      github.event_name == 'pull_request' || github.ref != 'refs/heads/main'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check for Vercel/Netlify/Supabase configs
        run: |
          set -e
          echo "Looking for deployment configs..."
          ls -la
          if [ -f "vercel.json" ]; then echo "Found vercel.json"; else echo "No vercel.json"; fi
          if [ -f "netlify.toml" ]; then echo "Found netlify.toml"; else echo "No netlify.toml"; fi
          if [ -d "supabase/functions" ]; then echo "Found Supabase Edge Functions"; else echo "No Supabase Edge Functions dir"; fi

  deployment_validation:
    name: Deployment Validation (main)
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: build-dist
          path: dist

      - name: Verify build output exists
        run: |
          test -d dist && echo "dist directory present" || (echo "dist missing" && exit 1)
          find dist -type f | wc -l

#   notify:
#     name: Notify (Slack optional)
#     runs-on: ubuntu-latest
#     needs: [lint, test, build]
#     if: always()
#     steps:
#       - name: Send Slack notification (if configured)
#         if: env.SLACK_WEBHOOK_URL != ''
#         uses: slackapi/slack-github-action@v1.27.0
#         env:
#           SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#           SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
#         with:
#           payload: |
#             {
#               "text": "${{ github.workflow }} for ${{ github.repository }} — ${{ job.status }} on ${{ github.ref }} (event: ${{ github.event_name }})"
#             }
